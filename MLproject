name: census-income-forecast
conda_env: conda.yaml

entry_points:
  main:
    parameters:

      steps:
        description: Comma-separated list of steps to execute (useful for debugging)
        type: str
        default: all

    command: "python main.py main.steps={steps}"